# Sucafina Sample Management — Feedback Status

Status of every stakeholder feedback item as of **22 September 2026** (round 10 built, deploy pending — see `docs/HANDOVER-2026-09-22-round10.md`). All items are live in production unless noted (round 6 rows 38–44 are built and verified locally and go live with the September deploy).

| # | Provider | Feedback | Lua Comment |
|---|----------|----------|-------------|
| 1 | Ivo Jr. Sarjanovic | Agent must request phone number of the end receiver if unavailable | ✅ Done. When a sample is logged for a client with no phone on file, the agent asks for it and saves it to the client book before proceeding (internal Sucafina offices exempt). It now does the same for a missing email. |
| 2 | Ivo Jr. Sarjanovic | Agent must clarify/know/ask if coffee needs a phytosanitary certificate before being sent — *Anicka: can we set it as "default" for a client?* | ✅ Done, including the per-client default. The agent asks the phyto question once the destination is known to be abroad. Each client can carry a standing default (set on their profile in the dashboard, or just tell the chat "Paulig always needs a phyto") — after that, new samples fill it in automatically and the question is skipped. |
| 3 | Ivo Jr. Sarjanovic | Rename "bulk" to "commercial" | ✅ Done. The book is labeled Commercial everywhere in the dashboard and chat; the agent still understands people who say "bulk". |
| 4 | Omar | Blend for each sample + historical data on blends ("What was the blend of the latest AA FAQ approved by Paulig") | ✅ Done. Every sample carries a Blend field, and the chat answers history questions exactly like that one. |
| 5 | Bernard | On sample status clarify — approved / rejected per sample, if possible reason for rejection | ✅ Done. Each sample shows approved / rejected / pending feedback, and rejected samples carry a Rejection Reason captured with the verdict. |
| 6 | Muki | Group samples to consignment and generate consignment number/reference | ✅ Done. Samples group into consignments with auto-issued CN numbers (CN-1000, CN-1001…), viewable on their own dashboard pages. |
| 7 | Muki | Assign location to samples (Westlands Lab, Thika Lab) | ✅ Done. Every sample carries a lab location (Westlands / Thika), filterable in the dashboard. |
| 8 | Muki | Assign sample-out consignment to location | ✅ Done. Consignments carry their own location, settable from chat or dashboard. |
| 9 | Bernard | Specify sample quantity in kgs — *Muki: I suggest in grams* | ✅ Done, in grams as Muki suggested. Stored in grams, displayed smartly (300 g, 1.5 kg), with sensible defaults by sample type (offer 200 g, type 300 g, PSS 1 kg). |
| 10 | Bernard | For pre-shipment samples add shipment month | ✅ Done. PSS samples carry a Shipment Month (auto-detected from phrases like "PSS June Shipment"). |
| 11 | Muki | For PSS/shipment samples add contract number | ✅ Done. PSS/shipment samples carry a Contract # field. |
| 12 | Muki | Generate automatic printable labels | ✅ Done. One-click printable labels with barcode from any sample or consignment page. |
| 13 | Omar | In Clients: list of all "approved samples" with Strategy Assigned, Blend composition, and Highlights (Blackcurrant bomb, Strict Clean Cups, Hibiscus character…) | ✅ Done. Each client page lists their approved samples with Strategy, Blend, and Highlights, and the chat can search by cup profile. |
| 14 | Omar | In Clients: average time we wait for feedback/approval + recording feedback on rejected samples | ✅ Done. The dashboard tracks average delivered→verdict turnaround, and rejection feedback is recorded per sample. |
| 15 | Omar | In Clients: statistics — % of Offer samples approved / Type samples approved / PSS approved | ✅ Done. Approval-rate chart by sample type (Offer / Type / PSS) on the dashboard. |
| 16 | Lynne | Include client specs, which will be a guide when sending out samples | ✅ Done. Each client carries specs (preferred grades, cup profile, moisture max, min score, notes); the agent consults them and flags off-spec sends. |
| 17 | Omar | Automatic email chaser if no response by client 7 days after they receive the sample | 🟡 Built, not switched on. The job (`client-feedback-chaser`) is complete but not registered on the agent — it enters one version after the details-chaser (one job per version, per the stability protocol). Clients with an email on file will get one chaser 7 days after delivery if no feedback has landed. |
| 18 | Omar | When asking Chat about a reference in the chaser, chat fails | ✅ Fixed. Sample lookup was made robust — chat now resolves refs from the chaser correctly. |
| 19 | Anicka | Stock management of samples / showing availability of a specific sample (e.g. alert that QC Westlands has only 300g) | ✅ Done. Samples carry grams-in-stock; the dashboard shows Low stock / Out of stock badges, and the agent warns before dispatch if the lab holds less than the send quantity ("Only 300g left against a 500g send — go ahead?"). Stock auto-decrements on dispatch. |
| 20 | Anicka | Automatic reply to the client about the dispatch of samples (including AWB or rider number), once marked "dispatched" | ✅ Live (email account connected 24 Aug). Once marked dispatched (from chat or dashboard), the client is automatically emailed the courier + AWB — one email per shipment, from "Sucafina Samples". |
| 21 | Muki | Assign user who placed the request | ✅ Done. Every sample records Requested By — auto-filled from whoever is chatting with the agent, editable in the dashboard. |
| 22 | Muki | Assign user who completed the request | ✅ Done. Completed By is stamped automatically when the dispatch is recorded. |
| 23 | Ivo Jr. Sarjanovic | Show status as first column, or freeze on the far right | ✅ Done. Status is frozen on the far right — always visible while scrolling across the table, on all three books. |
| 24 | Bavo | Connection to historical sales data — have we already sent the same lot-name sample to this client; if yes, how much and at what margin did we sell? | ⏳ Not yet built. Sample history per client/lot is available today ("have we sent this lot to X before?"), but volumes sold and margin live in the trading/sales system, which isn't connected — needs a data feed from that side. Scoped for the next round. |

| 25 | Ivo Jr. Sarjanovic (Teams, 24 Jul) | Add a column to flag importance/urgency of a sample | ✅ Done. Every sample carries a Priority (normal / urgent) — a red URGENT badge and "Urgent only" filter in all three books, editable in the drawer and on create. In chat, "send it urgently" flags it at creation and "mark TYPE-1006 as urgent" flags an existing one; urgent rows sort first on QC's pending list. |
| 26 | Ivo Jr. Sarjanovic (Teams, 24 Jul) | "How do you plan to send the sample without having the address?" — agent logged Folgers samples with no delivery address on file | ✅ Reworked in round 6 (see #38). The sample is logged, but the missing address is impossible to miss: an amber **Address needed** flag on the record and QC's ping (naming who was asked), a routed ask to the colleague who has it, a daily chase until it is saved, and one question at dispatch time. Adding an address to an existing contact completes that person's row (no duplicates). Internal Sucafina offices are exempt. |
| 27 | Sam (Teams, 24 Jul) | "Paulig" and "Gustav Paulig Ltd (NEW) Jan 23" are the same client — "merge them together"; agent replied it can't merge | ✅ Done. Duplicate clients can now be merged — from the client page ("Merge into…", with likely duplicates pre-suggested and a preview of what moves) or in chat ("merge them" → the agent proposes keeping the entry that has the address, confirms, then merges). Samples across all books and contacts move to the surviving entry (no duplicate people), its empty fields are filled from the duplicate, and the duplicate disappears from the list with a full audit trail. Internal Sucafina offices are never merged into clients. |

| 28 | Ivo Jr. Sarjanovic (round 5, 20 Aug) | Agent to log both: person logging the ask and person who requested it ("Logged by" / "Sales Trader"); if details are missing, open a group chat with the requester and the sales trader | ✅ Done. Every sample records both people — Logged by (stamped automatically from whoever is chatting with the bot, never asked) and Sales Trader (captured from the request, e.g. "Muki wants 300g AB to Beyers"; defaults to the logger when they're requesting for themselves). Both are columns on all three books. On the group chat: today the bot keeps collecting from the person logging AND sends the Sales Trader a direct 1:1 message listing exactly what's missing, so they can reply straight to the bot. Update 25 Aug: the platform now lets the bot post into a group chat it's been added to — next step is a desk group chat (QC + traders + bot) where missing-details callouts and new-request pings are posted for everyone to see, with answers still coming back 1:1. Bot-created per-sample group chats remain a platform roadmap item. |
| 29 | Ivo Jr. Sarjanovic (round 5, 20 Aug) | Bot to proactively communicate to Quality team when a new sample request has been added to the table in full | ✅ Live (24 Aug). The moment a request is logged in full, every Quality-team member with an email on file gets a Teams DM (email as fallback) with ref • quality • receiver • qty • who logged it and for which trader — flagged 🔴 when urgent. Checks run every 15 minutes, Mon–Sat 07:00–19:00 Nairobi; delivery is a Teams DM for anyone who has messaged the bot, email (from "Sucafina Samples") for everyone else. First live ping delivered 24 Aug. |
| 30 | Ivo Jr. Sarjanovic (round 5, 20 Aug) | Bot to proactively communicate to Sales Trader as the status progresses (preparing, sent to DHL, AWB added) | ✅ Live (24 Aug), together with #29. The Sales Trader on the record is notified automatically at each step — preparing (QC can now just tell the bot "pulling SL-8007"), dispatched (courier + AWB), and AWB added later. Works whether the update comes through the chat or the dashboard. Needs the trader's email on file — which #34 now captures automatically at intake. |
| 31 | QC (round 5, 20 Aug) | Add quantity column on the sample management tab | ✅ Already in place — the Qty column is on all three books, smart-formatted (300 g / 1.5 kg) and sortable. If it's missing on the screen you're looking at, send a screenshot and we'll align. |
| 32 | QC (round 5, 20 Aug) | Add Wells Fargo courier on the courier drop-down list | ✅ Done. Wells Fargo is now in the courier dropdown on all three books, and the chat recognises "Wells Fargo" / "Fargo" when a dispatch is reported (previously it was lumped under "other"). |
| 33 | Ivo Jr. Sarjanovic (round 5, 20 Aug) | WHO TO EMAIL: per-country Quality contact — for Kenya harriet.muthoni@sucafina.com and bernard.chege@sucafina.com, informed promptly on every new Kenya sample | ✅ Live. Harriet and Bernard are on the roster with exactly those emails and get a message within ~15 minutes of every fully-logged sample request (Teams DM once they've messaged the bot; email from "Sucafina Samples" until then). The roster is self-service on the dashboard's new **Team** page, so contacts can be changed without us. Per-country routing is parked until a non-Kenya desk onboards. |
| 34 | Ivo Jr. Sarjanovic (round 5, 20 Aug; clarified 25 Aug) | Keep the client's account manager (Sucafina sales side) in the loop — "ask the person straight away: who should be updated once we have the AWB or if there are follow-up questions? Please share the emails" | ✅ Live. Each client has an account manager on our side who is kept in the loop automatically — updated at every step (preparing, dispatched, AWB added) for every sample to that client, by Teams or email, with nobody forwarding anything. The first time a sample is logged for a client with no account manager on file, the bot asks exactly that question once — right after confirming the record — saves the person, and never asks again for that client. Anyone can be added on the spot: "keep Thomas in the loop on TYPE-1020" (that sample) or "Thomas handles Paulig" (all their samples). Account managers are also visible/editable on each client's page. |
| 35 | Brillian (round 5) | Allow user to edit the dispatch date | ✅ Done. The Dispatched On date is editable on every sample: open the row's side panel in any of the three books and pick the correct date. Dispatching still stamps the date automatically — this is for correcting it (e.g. recorded a day late). |
| 36 | Brillian (26 Aug) | Offer-sample refs should continue the desk's own numbering: start at SL-7459 | ✅ Done (API migration 015). New offer/specialty samples are issued SL-7459, SL-7460, … in the desk's unpadded format. Rows already issued as SL-80xx since July keep their refs. |
| 37 | Brillian (26 Aug) | Type-sample refs should start at TYPE-108 | ✅ Done (API migration 015). New type samples are issued TYPE-108, TYPE-109, …; rows already issued as TYPE-10xx keep their refs. |

| 38 | Ivo Jr. Sarjanovic (Teams, 8 Sep) | "The goal is not for you to hard block me because you're missing an address… I just get the request and send it to you. Then you chase people down for the details you need." (Beyers) | ✅ Built (round 6, agent v52). Log first, complete later: a request is written the moment the coffee, type, qty and receiver are known — an unknown client is added to the book from its name. A missing address shows as ⚠ on the chat card, an amber badge in the dashboard and a line on QC's ping; the bot asks ONE question ("who has it?"), routes the ask to that colleague by Teams or email (QC desk + the person logging copied), records who was asked, and the desk chases them every morning until the address is saved (v53). Before a dispatch on a flagged row QC is asked once where it went. The bot no longer uses share cards or any messaging tool of its own. |
| 39 | Dev (log sweep, 9 Sep) | Customers (Nestlé ×2, Itochu) had been saved as internal account managers through the loop-in question and were receiving internal status pings; real traders have no email on the roster | ✅ Built. The loop-in question now asks for a Sucafina colleague; a client's own email is saved on the client record instead. One-shot clean-up script moves the three back to their client records. The roster self-heals: whenever a colleague chats, their email is filled in on their roster row. |
| 40 | Harriet (round 6) | Any changes done from previous requests to be communicated to the quality team (e.g. email warning if a sample gets deleted) | ✅ Built (round 6). Every deletion of a sample, client or consignment, and every edit to a request's definition (quality, qty, client, receiver, country, priority, shipment month, contract, blend, phyto, type, which lot — outturn, stocklot, grower — and cancellation) by someone outside QC, reaches the Quality team within 15 minutes as one grouped message with old → new values and who did it. The dashboard now asks for your name so changes carry it. `docs/qc-whats-new-2026-09.md` is the team-facing note. |
| 41 | Harriet (round 6) | Labels: add Sucafina logo, and specifics on the outturn number | ✅ Built to Gloria's sample slips (11 Sep). The round Kenyacof mark on top, then one bold line each: **Stocklot · Outturn · Grower · Screen · Crop** (Specialty; the grower is the wet mill before the "/" of the name). Commercial, PSS and consignment labels use the same style, led by the ref; a PSS adds its contract and option. A small barcode with the ref sits under the slip so a bag still scans back to its row. New **Stocklot** field (dashboard and chat); outturn, grower and crop year can now be corrected in the dashboard. Crop year and stocklot are typed once per lot: a new sample of the same outturn carries them over, and when no crop year is on file the slip prints the season the sample was logged in (Oct–Sep, so 2025/2026 until 30 Sep). No outturn column on the Commercial book (Ivo, 10 Sep). |
| 42 | Harriet (round 6) | PSS must be sent 45 days before shipment; feed the PSS table from the SOL report; nest samples per contract into number of PSS and containers; client can reject one out of X | ✅ Built, reshaped on Harriet's answers (10 Sep): a contract owes N lettered PSS **options** (A, B, C…) of the client's usual size (Nespresso 1 kg, Zoegas 600 g, JDE 300 g, CK 500 g — from the contract or the client's last PSS, never a fixed 1 kg), with contract-derived refs (SSKE-104929A…) and the client's PO ref; the PSS due date is 45 days before shipment; "X of N approved" on the new **Contracts** page and the Commercial book, each option with its stage in Harriet's words. A rejection draws the replacement with the next letter (A–C → D–F) and tells you the new ref; a second rejection flags the contract **PSS replacement rejected** for QC and the account manager and still draws the next option. Reminders (14 / 7 / 0 days, then weekly while overdue) go to QC only. Send the SOL export (Excel/CSV; "quantity per sample" read as options × grams) to the bot and it shows a preview — nothing is written until you say go. Deleted refs are reused when they were the latest number. SOL gives the shipment as a month — its export writes "2026/08 All - 2026/08 All" — so the PSS is due 45 days before the 1st of that month (Ivo, 10 Sep). Once a contract's PSS is accepted a later export leaves it alone, and a contract that drops off a newer export is kept as it is, history and all. Mapping still to confirm with a real SOL export (Q7, with Gloria/Ivo). |
| 43 | Harriet (round 6) | Once an AWB is added, automatically look up the AWB for changes (delivered, stuck in customs…) — DHL and FedEx | ✅ Built (keys pending). DHL and FedEx are looked up every two hours in lab hours once Sucafina's developer keys are in `.env.prod`; until then the bot answers from the log and never invents a scan. |
| 44 | Harriet (round 6) | General clean-up: remove everything before 1 Aug 2026 | ✅ Built (Phase 3). Hidden, not deleted: reversible with one command. Every sample dated before 1 Aug 2026 is hidden from all three books and the legacy sheet; clients, the roster and the full audit trail are kept. Runs only on an explicit go after a database backup and a dry-run count review. Cutoff confirmed by Ivo (10 Sep): everything before 1 Aug, every book, every status — the Specialty book has the most old rows. |

| 45 | Lifecycle sketch (14 Sep) | The trader who asked must hear back the moment the AWB is added ("hey — your sample abc for CLIENT has an AWB, it'll be on its way soon"); the agent asks address + quantity up front; an AWB on file means "awaiting collection by DHL" | ✅ Built 14 Sep, not yet deployed. The Sales Trader and whoever logged the sample are now always in the loop (matched to the roster by name at send time, on top of the account manager and any loop-ins); the AWB ping reads "Your sample SL-7461 (AB FAQ) for Baba Coffee has a DHL AWB 1234567890 — it'll be on its way soon" and a dispatch typed seconds later no longer sends a second ping. Intake asks once, before writing: "Ok, I need a few details: what's Beyers' delivery address, and how much coffee in the sample?" — any answer, including "skip" or "ask Tommie", still logs the row immediately. A row with an AWB that has not left shows **awaiting collection** in the dashboard (filter included) and in chat answers. The loop-in question is only asked when neither the trader nor an account manager can be reached. See `docs/HANDOVER-2026-09-14-lifecycle-sketch.md`. |

| 46 | Ivo (sheet, Sep) | Can't add the agent to a 1:1 Teams conversation; forwarding shows two "Sample Managers" and fails | ✅ Built (round 10). A bot cannot join a 1:1 chat (Teams rule): forward the request to the colleague **and** the bot, or add it to a group chat. With app version 1.1.0 (five read permissions on Sucafina's own bot app) it reads the whole group chat, knows who said what, resolves colleagues from the chat roster ("ask Tommie" now works even if Tommie never DM'd the bot), posts missing-details asks into the chat, and imports the last 100 messages when added. The app is renamed **Sucafina Sample Manager**; IT removes the old shared "Lua Sample Manager" entry. Team guide: `docs/teams-loop-in-the-bot.md`. |
| 47 | Ivo (sheet, Sep) | Only sucafina.com emails allowed on the Team CRM | ✅ Built (round 10). The Team page and the API both refuse non-@sucafina.com addresses ("a client's contact goes on the client record"); the agent already refused them. |
| 48 | Ivo (sheet, Sep) | Allow multiple users to be mapped to a single sample for updates | ✅ Built (round 10). Already possible in chat ("keep Thomas in the loop on TYPE-1020", up to 20 people); the sample drawer now has an **In the loop** section to see, add and remove them. |
| 49 | Harriet (sheet + 21 Sep call) | The bot assigned the same reference to different samples (TYPE-113); refs the desk types are not recognised | ✅ Built (round 10). A ref now names the **coffee**, exactly as in the Sample Chaser sheet: the same coffee sent again keeps its ref ("same coffee — reusing TYPE-973, 4th send"), a different coffee under a typed ref is stopped ("TYPE-113 is AB FAQ; this is C FAQ — new ref, OK?"), and a typed free ref is honoured and the counter moves past it. Follow-ups by ref ask "which receiver?" only when more than one send is open. Dashboard: ×N pill on shared refs, a **Coffees** view (one row per coffee, expand to its sends), and a Related tab in the drawer. Existing clashes are separated by a one-shot script that tells QC old → new refs. |
| 50 | Harriet (sheet) | Copy kenyaqc@sucafina.com on all ping emails | ✅ Built (round 10). Both QC mailboxes (kenyacof.specialtyqc@ and kenyaqc@) are copied once per email. |
| 51 | Harriet (sheet + call) | More info on ping emails: number of samples, client emails for new clients | ✅ Built (round 10). Several coffees to one client in one request are logged as an **order** (CN-####) and pinged once: "New order CN-1012 for EDMAX — 3 samples", one line per ref, then the client's contact · email · phone, flagged 🆕 NEW CLIENT when the client was added with the request, plus type and country and a dashboard link. |
| 52 | Brillian (21 Sep call) | Not receiving the ping emails | ✅ Data fix: her roster row had no email (rows without an email are silent by design). Added on the Team page — she now gets every ping. |
| 53 | Mayank (21 Sep call) | Forwarding a request to the bot | ✅ See #46. |

**Summary:** 50 of 53 items complete or built — everything is live, including outbound email (account connected 24 Aug, sender "Sucafina Samples"), which switched on items 17 & 20 and the email fallback for 29/30. See `round5-happy-path.md` for the team walkthrough. The only open item is 24 (Bavo), new scope requiring a feed from the trading/sales system.

**Round-5 activation checklist (what we still need from the team):**
1. Trader emails (Ivo, Omar, Muki, Brian, Gloria) — no longer blocking: the bot now asks for them at intake (#34) and they fill in as samples get logged. Sharing them up-front just gets everyone covered sooner. Harriet's and Bernard's are already on file.
2. Confirm who counts as "Quality team" (chat history suggests Gloria/Brian may be lab-side and Anička trader-side — the opposite of our current roster) and whether Margaret Mbugua should be included.
3. Everyone sends the bot one first DM in Teams so pings arrive in Teams rather than by email (until then, pings fall back to email).
